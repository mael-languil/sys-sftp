<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xmlns:sftp="http://www.mulesoft.org/schema/mule/sftp"
	xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
http://www.mulesoft.org/schema/mule/sftp http://www.mulesoft.org/schema/mule/sftp/current/mule-sftp.xsd
http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
	<sub-flow name="GetInvoices" doc:id="c9f370d5-89a8-417d-bfc0-17cceb2a3892" >
		<flow-ref doc:name="SetupParameters" doc:id="de03b459-32dd-430d-9168-aaa2167e4c6a" name="SetupParameters" />
		<sftp:read doc:name="Read" doc:id="d4e0cc48-9c11-4cdf-a586-e3fbd7e2a0e0" config-ref="SFTP_Config" path="invoices.json" outputMimeType="application/json" />
		<ee:transform doc:name="Transform Message" doc:id="83b783ca-5aa7-481a-bebd-a3b0a237bd3b" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/json

// ================= VARIABLES =================

var currencyParam = vars.currencyQP default null
var invoiceId     = vars.id default null

// ================= FUNCTION =================

// Fonction pour convertir epoch seconds -> DateTime ISO
fun toDateTime(epoch) =
    (epoch as Number) as DateTime {unit: "seconds"}

// ✅ Ton mapping EXACT, inchangé
fun mapInvoice(invoiceWrapper) = do {

    var inv = invoiceWrapper.data
    var lines = inv.lines.data default []

    ---
    {
        id: inv.id,

        total: 
            (lines map $.amount)
                reduce ((item, acc = 0) -> acc + item),

        currency: upper(inv.currency),

        date: toDateTime(inv.created),

        customer: {
            id: inv.customer,
            name: inv.customer_name,
            email: inv.customer_email,
            phone: inv.customer_phone,
            city: inv.customer_address.city,
            country: inv.customer_address.country,
            postalCode: inv.customer_address.postal_code
        },

        lineItems: 
            lines map (line) -> {
                id: line.id,
                amount: line.amount
            }
    }
}

//
// ================= FILTRAGE =================
//

var safePayload =
    if (payload is Array)
        payload
    else
        []

var afterCurrency =
    if (currencyParam == null)
        safePayload
    else
        safePayload filter (
            upper($.data.currency) == upper(currencyParam)
        )

var afterId =
    if (invoiceId == null)
        afterCurrency
    else
        afterCurrency filter (
            $.data.id as String == invoiceId as String
        )

//
// ================= OUTPUT =================
//

---
if (invoiceId != null)
    if (isEmpty(afterId))
        null
    else
        mapInvoice(afterId[0])
else
    afterId map (item) -> mapInvoice(item)

]]></ee:set-payload>
			</ee:message>
		</ee:transform>
		<logger level="INFO" doc:name="Logger" doc:id="0a454c3e-5349-49fb-af33-3d9ce66d5059" />
	</sub-flow>
	<sub-flow name="SetupParameters" doc:id="58bf89aa-4027-4343-9da6-93d3c2a4a2b3" >
		<set-variable value="#[attributes.queryParams.currency default null]" doc:name="currencyQP" doc:id="32967277-6bc7-4fde-935e-0ed74fe708bb" variableName="currencyQP" />
		<set-variable value="#[attributes.uriParams.id default null]" doc:name="id" doc:id="64995db8-f905-43ce-a1cd-701c8a52575b" variableName="id" />
	</sub-flow>
</mule>
